add_subdirectory(EventsRegistry)
add_subdirectory(DeviceStatus)
add_subdirectory(NetworkEnvironment)
if (NOT WIN32)
  add_subdirectory(MobileConnection)
  add_subdirectory(UnitsOfMeasure)
endif()
add_subdirectory(WebEvent)